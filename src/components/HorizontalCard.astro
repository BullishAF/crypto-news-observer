---
const { title, img, desc, url, badge, tags, target = '_blank' } = Astro.props;
---

<div class="rounded-lg bg-base-300 hover:shadow-2xl transition ease-in-out hover:scale-[102%]">
  <a href={url} target={target}>
    <div class="hero-content flex-col md:flex-row">
      <img src={img} alt={title} class="max-w-full md:max-w-[12rem] rounded-lg" />
      <div class="grow w-full">
        <h1 class="text-xl font-bold">
          {title}
          {badge && <div class="badge badge-primary mx-2">{badge}</div>}
        </h1>
        <p class="py-1 text-1xl">{desc}</p>
        <div class="card-actions justify-end">
          {tags && tags.map((tag) => <div class="badge badge-outline">{tag}</div>)}
        </div>
      </div>
    </div>
  </a>
</div>
